<%= form_for(@billcategory) do |f| %>
  <% if @billcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@billcategory.errors.count, "error") %> prohibited this billcategory from being saved:</h2>

      <ul>
      <% @billcategory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
